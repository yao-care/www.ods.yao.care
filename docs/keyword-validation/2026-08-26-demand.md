# 關鍵字可行性驗證（2026-08-26）第四波：改用實測需求量選字

前三波（`2026-08-20-pinned-14.md`、`2026-08-26-market-vocabulary.md`）都靠 **SERP 觀察**：
每字實搜一次，看前排是誰、在回答什麼。那個方法判斷得了「意圖」與「供給密度」，
但三份文件都留著同一句沒有回答的問題：**「這些字的實際搜尋量沒有數據。」**

這一波把那句話補上。

## 方法

`/root/seo-ops/bin/keyword-demand.mjs`（Bing Webmaster Keyword API，唯讀，
2026-08-26 因 arthurs.tw 的同型問題而建）。參數 `--country tw --language zh-TW`，
區間 2026-05-28 ~ 2026-08-25，取 `Impressions`（精準）與 `BroadImpressions`（含變體）。

⚠️ **判讀邊界，先講清楚再看數字**：

1. 這是 **Bing** 的量不是 Google。台灣市佔低，**只能看相對大小與「是不是 0」**，不能當流量預估。
2. **多字組合的 0 不可靠**。實測「公文格式」300 但「公文格式 word」0 —— 量在主詞上，
   Bing 沒有把組合切出來。**單字頭部詞的 0 才可靠**。
3. 有量 ≠ 打得贏。要同時看 SERP 前排是不是我們搶得到的（前三波做的就是這件事）。

## 一、先量原本釘著的 20 個字

**只有 4 個有量。**

| 字 | exact | 站上對應頁 |
|---|---|---|
| 存證信函範例 | 704 | `/citizens/certified-letter*` |
| 公文用語 | 597 | `/usage/` |
| 存證信函格式 | 376 | `/citizens/certified-letter*` |
| 公文範例 | 247 | `/cases/` |

其餘 16 個是 0，包括前兩波判定「可追」的
**公文格式 word、公文範本下載、開會通知單範例、補助核銷公文範例**，
以及第三波剛加的 **公文大腦、AI 寫公文、公務員可以用ChatGPT寫公文嗎、公文速別怎麼填、公文核稿會稿流程**。

其中多字組合（公文格式 word、存證信函格式 word、陳情書 訴願 差別…）依邊界 2 不能當真 0；
但**單字型的 0 是實訊**。

## 二、再量 130 個候選字，最大的需求根本不在原清單裡

| 字 | exact | 站上原本有沒有 |
|---|---|---|
| **數字大寫** | **7,829** | ❌ 只有 `/writing/` 一節講「該用哪一種」 |
| 公文系統 | 2,317 | ❌（第三波刻意不追，見下） |
| 存證信函 | 2,058 | ✅ 6 頁 |
| 委託書 | 1,865 | ❌ 完全沒有 |
| **國字大寫** | **1,048** | ❌ |
| 車禍和解書 | 982 | ❌ |
| 切結書 | 958 | ❌ |
| 請款單 | 943 | ❌（我們叫「請撥」） |
| **大寫數字** | **936** | ❌ |
| 和解書 | 829 | ❌ |
| 委託書範例 | 763 | ❌ |
| 簽呈 | 732 | ⚠️ 有頁，但站上一律叫「簽」 |
| 文書處理手冊 | 732 | ⚠️ 引用很多次，沒有落地頁 |
| 授權書 | 536 | ❌ |
| 切結書範例 | 399 | ❌ |
| 和解書範例 | 341 | ❌ |
| 請購單 | 322 | ⚠️ 有頁，但標題寫「請購」 |
| 公文格式 | 300 | ✅ `/templates/` |
| 聲明書 | 293 | ❌ |
| 支票大寫 | 294 | ❌ |
| 金額大寫 | 245 | ❌ |
| 電子公文 | 202 | ✅ `/product/` |
| 陳情書 | 112 | ✅ |
| 訴願 | 103 | ✅ |

## 三、對照 GSC：有量的字，我們一次曝光都沒有

近 3 個月（2026-06-01 ~ 08-26）全網域 1,469 個不重複查詢字，逐一比對上面每一個有量的字：

```
數字大寫   — 一次曝光都沒有        委託書   — 一次曝光都沒有
國字大寫   — 一次曝光都沒有        切結書   — 一次曝光都沒有
大寫數字   — 一次曝光都沒有        和解書   — 一次曝光都沒有
公文用語   — 一次曝光都沒有        簽呈     — 一次曝光都沒有
存證信函範例 — 一次曝光都沒有      公文系統 — 一次曝光都沒有
存證信函格式 — 一次曝光都沒有      公文格式 — 一次曝光都沒有
公文範例   → 只有「採購公文範例」1 次曝光、第 46 名
```

**這才是曝光只有個位數的直接原因**：不是內容不夠，是內容押的字沒有人搜；
而有人搜的字，我們沒有頁面或用詞對不上。

## 四、本波做了什麼

| 動作 | 內容 |
|---|---|
| 新增 `/numbers/` | 數字大寫群（7,829＋1,048＋936＋294＋245 ≈ **10,352**）是量到的最大一群。頁面給對照表、進位與補零的範例、公文什麼時候用大寫，以及**核銷憑證的大寫規定原文**（《政府支出憑證處理要點》第十三點）。含一個換算器 |
| 換算實作 | `src/data/uppercase-number.js`，伺服器端與瀏覽器端吃同一份；漸進增強（沒有 JS 時對照表與範例照樣可讀，只是沒有輸入框） |
| 新守門 | `scripts/check-numbers.mjs`（在 build 內）驗 26 個邊界案例與 4 個拒絕輸入 —— 換算算錯不會有畫面異常，使用者會把錯的字串抄進核銷憑證 |
| 用詞落差 | 「簽呈」732 但站上一律叫「簽」→ 6 則簽的案例標題改成「簽呈」，`/doc-types/` 註明兩者是同一件事；「請購單」322、「請款」943 → 兩則案例標題改用市場說法 |
| 釘選字重建 | seo-ops `sites/ods.yao.care.json` 的 pinned 全部換成**量得到且站上有對應頁**的字 |

## 五、還沒做的：民間書件那一群

委託書 1,865＋委託書範例 763＋授權書 536＋切結書 958＋切結書範例 399＋
和解書 829＋車禍和解書 982＋和解書範例 341＋聲明書 293 ≈ **6,966**，站上一頁都沒有。

這一群**不是加幾頁就好**：應用端的 `citizen` 族群是「民眾對機關」（申請書／陳情書／申訴書／說明書），
`notice` 族群是存證信函。委託書、切結書、和解書、聲明書是**民間對民間**的書件，
要開一個新的文書族群（段名、必填欄位、檢核、提示詞、Word 範本都要配）。

**2026-08-26 用戶拍板全做**，已開 `private` 族群並上線委託書、授權書、切結書、聲明書、和解書
（`/private-documents/`，8 則範例）。

段名不是想出來的，是從官方制式範本反推：臺南市地政「切結書（範本）」、公平會
「切結書（範例1）」、臺中市北區公所「代辦委託書」。查法條原文的過程改掉了兩件事：

- **加了一條檢核**：民法第 534 條的六類行為（不動產出賣或設定負擔、不動產租賃逾二年、
  贈與、和解、起訴、提付仲裁）須有特別之授權，行政程序法第 24 條的「申請之撤回」亦同。
  這是委託書最常見的無效原因。
- **改掉一個我寫錯的**：原本寫「監理機關多半有自己的制式委託書」，但道路交通安全規則
  第 16 條對「委託買賣業以外之他人代辦過戶」要的是代辦人身分證＋車主有效駕駛執照或
  健保卡或護照，一張委託書辦不成。**這一區寫錯會害到使用者，動文案前先查法條。**

**和解書那兩則的作法是刻意的**：不只給範本，把保護條款做成預設。查證後最有力的一組對照是
臺灣新竹地方法院 110 年度簡上字第 111 號（一份 3,000 元的和解書，因為「並無其他損害賠償
請求權保留之陳述或記載」，關掉事後 1,253 萬元的請求；用的還是流通的官方範本文字）
與臺灣高等法院 108 年度上字第 904 號（和解書載明「僅就系爭機車損害」，人身損害因此保住）。
於是段名獨立出「拋棄範圍與保留事項」一段，並加兩條檢核：`settlement_scope`（critical，
不准只寫概括拋棄）與 `settlement_insurance`（warn，車禍案要交代強制險含不含）。
烘出來的車禍那則確實寫成「本件僅就車輛損害和解，人身損害之請求權不在本件範圍，仍予保留」
且「強制汽車責任保險給付不包含在本和解金額內」——比它要取代的那份官方範本安全。

另外兩件查證後寫進頁面的：「委託書」與「授權書」在台灣法制上是同一件事的不同叫法
（公證法叫授權書、行政程序法叫委任書、土地登記規則與公司法叫委託書，官方表格自己也混用）；
**公開發行公司的股東會委託書不能自製**（用紙以公司印發者為限，否則代理表決權不予計算）。

## 五之二、順帶量到的其他書件（本波不做，留給下一輪）

量民間書件時把相鄰的書件一起量了，結果有幾群比預期大。**這些本波刻意不做**，
記在這裡是為了下一輪不必重新發現：

| 群 | 字與量 | 判斷 |
|---|---|---|
| 職場證明 | 在職證明 2,543／離職證明書 1,889／服務證明書 375 ≈ **4,807** | 量很大，但這是**雇主開給員工**的表單，多半用公司自己的格式；離本站「正式書件寫作」的主題最遠。要做要先想清楚它算不算同一個站 |
| 收據與領據 | 收據 949／收據範本 647／**領據 700** ≈ 2,296 | **領據直接落在補助核銷**（本站知識庫那條「核銷應檢附…領據」講的就是它），而且它的金額**必須國字大寫**，跟 `/numbers/` 天生相接。官方要件已查證：請款單位全銜、受領事由、支付機關、金額國字大寫、日期、統一編號、匯款帳號，並蓋印信與理事長／會計／出納章。**下一輪優先做這個** |
| 借據與本票 | 借據範本 858／本票 468／借據 435／本票範本 170 ≈ 1,931 | 金錢借貸，法律後果重（本票有強制執行力），要做得非常小心 |
| 租賃 | 租賃契約 390／房屋租賃契約書 336 ≈ 726 | 已有內政部制式契約範本與應記載事項，我們重做價值低 |
| 讓渡書／同意書 | 同意書 312／讓渡書 0 | 「同意書」語意太泛，看不出意圖 |
| 拋棄繼承 | 623 | 那是法院程序不是書件寫作，不追 |

## 六、這一波真正改掉的是流程

前三波選字靠「我覺得承辦會搜什麼」＋SERP 觀察，**沒有量**。往後選字的順序固定成：

1. `node /root/seo-ops/bin/keyword-demand.mjs --file <候選字.json>` 先量，**單字頭部詞判 0 就不做**
2. 有量的字再實搜一次看 SERP 前排是不是搶得到的（前三波的方法）
3. `grep -r` 自家 repo，確認站上用的是不是同一個詞
4. 上線後用 GSC 的實際 query 回頭校正（回頭校正日 **2026-09-20**）

## 沒有回答的問題

- Bing 的量與 Google 的量差多少，不知道。只能當相對指標。
- 「數字大寫」的搜尋者有多少比例是公務／核銷情境（我們答得好的那半），
  多少是支票、發票、單據（我們只答得到一半）。等 GSC 累積後看實際 query 分佈。
- 站外連結仍然只有一條，這一波沒有動到。
