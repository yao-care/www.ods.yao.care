---
const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
---

<footer>
  <div class="inner">
    <p class="disclaimer">
      本站所有公文範例的機關名稱、人名、地址與案號皆為虛構，僅供格式參考，不對應任何真實個案。
    </p>
    <nav aria-label="頁尾導覽">
      <a href={`${base}cases/`}>公文範例</a>
      <a href={`${base}security/`}>資料處理方式</a>
      <a href={`${base}apply/`}>申請試用</a>
    </nav>
    <p class="org">藥提醒科技有限公司</p>
  </div>
</footer>

<style>
  footer {
    margin-top: 4rem;
    border-top: 1px solid var(--color-line);
    background: var(--color-bg-light);
  }
  .inner {
    max-width: 72rem;
    margin: 0 auto;
    padding: 2rem 1.25rem 3rem;
    display: grid;
    gap: 1rem;
  }
  .disclaimer {
    font-size: var(--text-xs);
    color: var(--color-ink-soft);
    max-width: 48rem;
  }
  nav {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
  }
  nav a {
    font-size: var(--text-sm);
    color: var(--color-primary);
  }
  .org {
    font-size: var(--text-xs);
    color: var(--color-ink-muted);
  }
</style>
